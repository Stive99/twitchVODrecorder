export async function startRecording(): Promise<void> {
	throw new Error(
		"Live recording is disabled. Bot now works in VOD-only mode.",
	);
}

export async function stopRecording(): Promise<void> {
	throw new Error(
		"Live recording is disabled. Bot now works in VOD-only mode.",
	);
}
